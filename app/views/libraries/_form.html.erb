<%= simple_form_for(@library) do |f| %>
  <%= f.error_notification %>

  <%= f.input :name %>
  <%= f.input :address, :input_html => { :id => 'geo-address' }, placeholder: '' %>

  <%= f.button :submit %>
<% end %>
